# 抖音热搜监控系统 - 使用指南

## 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 启动服务
```bash
python3 run.py
```

### 3. 访问界面
打开浏览器访问: **http://localhost:5001**

---

## 功能说明

### 实时热榜
- 左侧面板显示实时热搜榜单
- 点击任意热搜词可查看趋势图表
- 🔥 标记表示"新上榜"

### 热词对比工作台
- 在中间面板输入热词
- 最多添加 5 个热词进行对比
- 图表自动统一时间尺度

### 监控设置
- **抓取间隔**: 1-60 分钟
- **自动刷新**: 10-300 秒
- **历史保留**: 1-30 天

---

## 数据存储

```
data/
├── settings.json      # 配置文件
├── douyin.db         # SQLite 数据库
└── records/          # JSON 快照
    └── 2026-01-17/
        ├── 16-30.json
        └── 16-40.json
```

---

## API 接口

| 接口 | 说明 |
|------|------|
| `GET /api/hot` | 获取当前热榜 |
| `GET /api/rising` | 获取上升趋势 |
| `GET /api/trend/<word>` | 获取热词趋势 |
| `GET /api/status` | 获取系统状态 |
| `GET/POST /api/settings` | 获取/更新设置 |
| `POST /api/refresh` | 手动刷新数据 |
| `GET /api/records` | 获取历史日期列表 |

---

## 常见问题

### 端口被占用
```bash
lsof -ti:5001 | xargs kill -9
```

### API 获取失败
系统会自动回退到演示数据，右上角显示"演示模式"。

---

## 技术栈

- **后端**: Flask + APScheduler
- **前端**: 原生 JS + ECharts
- **数据**: SQLite + JSON 快照

---

**⚠️ 仅供学习研究使用**
